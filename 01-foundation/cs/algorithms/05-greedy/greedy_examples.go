package main

// MinCoinsGreedy đổi tiền theo chiến lược greedy.
// TODO: triển khai thân hàm với danh sách mệnh giá cho trước (đã sort giảm dần).
func MinCoinsGreedy(amount int, denominations []int) int {
	// TODO: implement greedy coin change (giả sử hệ mệnh giá "đẹp")
	return 0
}

// MaxNonOverlappingIntervals chọn tối đa số interval không giao nhau.
// TODO: triển khai thân hàm (gợi ý: sort theo end time).
type Interval struct {
	Start int
	End   int
}

func MaxNonOverlappingIntervals(intervals []Interval) int {
	// TODO: implement activity selection greedy
	return 0
}

